{% block body %}
{{ 'mail.reset_password.body'|trans({
    '%homepage%' : absolute_url(path('front')),
    '%url%' : absolute_url(path('plejeune_user_password_reset_form',{'token' : user.token}))}
    , 'PLejeuneUser')|raw }}
{% endblock %}
