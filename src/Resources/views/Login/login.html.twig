{% extends 'security.html.twig' %}

{% block body %}
    <form action="{{ path('darkanakin41_user_login') }}" method="post">
        <div class="box login">
            <div class="header">
                <h1 class="title">
                    <i class="fas fa-sign-in-alt" aria-hidden="true"></i> {% block title %}{{ 'login'|trans({}, 'Darkanakin41User') }}{% endblock %}
                </h1>
            </div>
            <div class="body">
                {% if error %}
                    <div class="callout alert">
                        {% if "error." in error.messageKey %}
                            {{ error.messageKey|trans({}, 'Darkanakin41User') }}
                        {% else %}
                            {{ error.messageKey }}
                        {% endif %}
                    </div>
                {% endif %}
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"/>
                <label>
                    {{ 'username'|trans({}, 'Darkanakin41User') }}
                    <input type="text" id="username" name="_username" value="{{ last_username }}" class="form-control"/>
                </label>

                <label>
                    {{ 'password'|trans({}, 'Darkanakin41User') }}
                    <input type="password" id="password" name="_password" class="form-control"/>
                </label>

                <label>

                    <input type="checkbox" id="remember_me" name="_remember_me" />
                    <label for="remember_me">{{ 'remember_me'|trans({}, 'Darkanakin41User') }}</label>
                </label>
            </div>
            <div class="footer">
                <div class="grid grid-x grid-padding-x">
                    <div class="cell medium-6">
                        <button type="submit" class="button success">
                            <i class="fa fa-lock" aria-hidden="true"></i> {{ 'action.login'|trans({}, 'Darkanakin41User') }}
                        </button>
                    </div>
                    <div class="cell medium-6 text-right">
                        <small>
                            <a href="{{ path('darkanakin41_user_password_reset_request') }}">
                                {{ 'forgotten_password'|trans({}, 'Darkanakin41User') }}
                            </a>
                            <br/>
                            <a href="{{ path('darkanakin41_user_registration') }}">
                                {{ 'registration'|trans({}, 'Darkanakin41User') }}
                            </a>
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </form>
{% endblock %}
